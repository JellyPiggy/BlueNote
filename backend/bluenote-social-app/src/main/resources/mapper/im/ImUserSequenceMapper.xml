<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.social.im.infrastructure.mapper.ImUserSequenceMapper">

    <resultMap id="ImUserSequenceResultMap" type="com.bluenote.social.im.infrastructure.entity.ImUserSequenceEntity">
        <id column="user_id" property="userId"/>
        <result column="current_seq" property="currentSeq"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insertIgnore" parameterType="com.bluenote.social.im.infrastructure.entity.ImUserSequenceEntity">
        INSERT IGNORE INTO bluenote_im.im_user_sequence (
            user_id, current_seq, created_at, updated_at
        ) VALUES (
            #{userId}, #{currentSeq}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="selectForUpdate" resultMap="ImUserSequenceResultMap">
        SELECT user_id, current_seq, created_at, updated_at
        FROM bluenote_im.im_user_sequence
        WHERE user_id = #{userId}
        FOR UPDATE
    </select>

    <update id="updateCurrentSeq">
        UPDATE bluenote_im.im_user_sequence
        SET current_seq = #{currentSeq},
            updated_at = #{updatedAt}
        WHERE user_id = #{userId}
    </update>
</mapper>
