<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.social.im.infrastructure.mapper.ImConversationMapper">

    <resultMap id="ImConversationResultMap" type="com.bluenote.social.im.infrastructure.entity.ImConversationEntity">
        <id column="conversation_id" property="conversationId"/>
        <result column="conversation_type" property="conversationType"/>
        <result column="single_key" property="singleKey"/>
        <result column="current_seq" property="currentSeq"/>
        <result column="last_message_id" property="lastMessageId"/>
        <result column="last_message_at" property="lastMessageAt"/>
        <result column="conversation_status" property="conversationStatus"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="ImConversationColumns">
        conversation_id, conversation_type, single_key, current_seq, last_message_id,
        last_message_at, conversation_status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.bluenote.social.im.infrastructure.entity.ImConversationEntity">
        INSERT INTO bluenote_im.im_conversation (
            conversation_id, conversation_type, single_key, current_seq, last_message_id,
            last_message_at, conversation_status, created_at, updated_at
        ) VALUES (
            #{conversationId}, #{conversationType}, #{singleKey}, #{currentSeq}, #{lastMessageId},
            #{lastMessageAt}, #{conversationStatus}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="selectById" resultMap="ImConversationResultMap">
        SELECT <include refid="ImConversationColumns"/>
        FROM bluenote_im.im_conversation
        WHERE conversation_id = #{conversationId}
    </select>

    <select id="selectByIdForUpdate" resultMap="ImConversationResultMap">
        SELECT <include refid="ImConversationColumns"/>
        FROM bluenote_im.im_conversation
        WHERE conversation_id = #{conversationId}
        FOR UPDATE
    </select>

    <select id="selectBySingleKey" resultMap="ImConversationResultMap">
        SELECT <include refid="ImConversationColumns"/>
        FROM bluenote_im.im_conversation
        WHERE single_key = #{singleKey}
    </select>

    <select id="selectByIds" resultMap="ImConversationResultMap">
        SELECT <include refid="ImConversationColumns"/>
        FROM bluenote_im.im_conversation
        WHERE conversation_id IN
        <foreach collection="conversationIds" item="conversationId" open="(" separator="," close=")">
            #{conversationId}
        </foreach>
    </select>

    <update id="updateLastMessage">
        UPDATE bluenote_im.im_conversation
        SET current_seq = #{currentSeq},
            last_message_id = #{lastMessageId},
            last_message_at = #{lastMessageAt},
            updated_at = #{updatedAt}
        WHERE conversation_id = #{conversationId}
    </update>
</mapper>
