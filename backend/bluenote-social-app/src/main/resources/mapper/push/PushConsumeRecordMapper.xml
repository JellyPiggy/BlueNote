<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.social.push.infrastructure.mapper.PushConsumeRecordMapper">

    <resultMap id="PushConsumeRecordResultMap" type="com.bluenote.social.push.infrastructure.entity.PushConsumeRecordEntity">
        <id column="id" property="id"/>
        <result column="consumer_group" property="consumerGroup"/>
        <result column="event_id" property="eventId"/>
        <result column="topic" property="topic"/>
        <result column="event_type" property="eventType"/>
        <result column="biz_key" property="bizKey"/>
        <result column="envelope_json" property="envelopeJson"/>
        <result column="consume_status" property="consumeStatus"/>
        <result column="retry_count" property="retryCount"/>
        <result column="error_message" property="errorMessage"/>
        <result column="consumed_at" property="consumedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.bluenote.social.push.infrastructure.entity.PushConsumeRecordEntity">
        INSERT INTO bluenote_push.push_consume_record (
            id, consumer_group, event_id, topic, event_type, biz_key, envelope_json, consume_status,
            retry_count, error_message, consumed_at, created_at, updated_at
        ) VALUES (
            #{id}, #{consumerGroup}, #{eventId}, #{topic}, #{eventType}, #{bizKey}, #{envelopeJson}, #{consumeStatus},
            #{retryCount}, #{errorMessage}, #{consumedAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="selectByGroupAndEvent" resultMap="PushConsumeRecordResultMap">
        SELECT id, consumer_group, event_id, topic, event_type, biz_key, envelope_json, consume_status,
               retry_count, error_message, consumed_at, created_at, updated_at
        FROM bluenote_push.push_consume_record
        WHERE consumer_group = #{consumerGroup}
          AND event_id = #{eventId}
    </select>

    <update id="markSuccess">
        UPDATE bluenote_push.push_consume_record
        SET consume_status = 'SUCCESS',
            error_message = NULL,
            consumed_at = #{consumedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="markSkipped">
        UPDATE bluenote_push.push_consume_record
        SET consume_status = 'SKIPPED',
            error_message = #{reason},
            consumed_at = #{consumedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="markFail">
        UPDATE bluenote_push.push_consume_record
        SET consume_status = 'FAIL',
            retry_count = retry_count + 1,
            error_message = #{errorMessage},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
</mapper>
