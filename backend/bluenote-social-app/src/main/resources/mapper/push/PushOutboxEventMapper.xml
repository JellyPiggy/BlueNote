<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.social.push.infrastructure.mapper.PushOutboxEventMapper">

    <insert id="insert" parameterType="com.bluenote.social.push.infrastructure.entity.PushOutboxEventEntity">
        INSERT INTO bluenote_push.push_outbox_event (
            event_id, event_type, aggregate_id, payload, send_status, retry_count, next_retry_at, created_at, updated_at
        ) VALUES (
            #{eventId}, #{eventType}, #{aggregateId}, #{payload}, #{sendStatus}, #{retryCount}, #{nextRetryAt}, #{createdAt}, #{updatedAt}
        )
    </insert>
</mapper>
