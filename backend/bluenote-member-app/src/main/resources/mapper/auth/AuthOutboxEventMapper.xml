<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.member.auth.infrastructure.mapper.AuthOutboxEventMapper">

    <insert id="insert" parameterType="com.bluenote.member.auth.infrastructure.entity.AuthOutboxEventEntity">
        INSERT INTO bluenote_auth.auth_outbox_event (
            id, event_id, event_type, aggregate_id, payload, status, retry_count,
            next_retry_at, created_at, updated_at
        ) VALUES (
            #{id}, #{eventId}, #{eventType}, #{aggregateId}, #{payload}, #{status}, #{retryCount},
            #{nextRetryAt}, #{createdAt}, #{updatedAt}
        )
    </insert>
</mapper>
