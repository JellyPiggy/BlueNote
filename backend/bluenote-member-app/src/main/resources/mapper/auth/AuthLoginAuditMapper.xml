<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.member.auth.infrastructure.mapper.AuthLoginAuditMapper">

    <insert id="insert" parameterType="com.bluenote.member.auth.infrastructure.entity.AuthLoginAuditEntity">
        INSERT INTO bluenote_auth.auth_login_audit (
            id, user_id, username, action, result, fail_reason, ip, device_id,
            platform, app_version, trace_id, created_at
        ) VALUES (
            #{id}, #{userId}, #{username}, #{action}, #{result}, #{failReason}, #{ip}, #{deviceId},
            #{platform}, #{appVersion}, #{traceId}, #{createdAt}
        )
    </insert>
</mapper>
