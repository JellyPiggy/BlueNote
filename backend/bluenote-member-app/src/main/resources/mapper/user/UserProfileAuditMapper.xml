<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.member.user.infrastructure.mapper.UserProfileAuditMapper">

    <insert id="insert" parameterType="com.bluenote.member.user.infrastructure.entity.UserProfileAuditEntity">
        INSERT INTO bluenote_user.user_profile_audit (
            id, user_id, field_name, old_value_mask, new_value_mask,
            operator_id, operator_type, trace_id, created_at
        ) VALUES (
            #{id}, #{userId}, #{fieldName}, #{oldValueMask}, #{newValueMask},
            #{operatorId}, #{operatorType}, #{traceId}, #{createdAt}
        )
    </insert>
</mapper>
