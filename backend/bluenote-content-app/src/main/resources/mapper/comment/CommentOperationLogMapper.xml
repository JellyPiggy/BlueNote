<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluenote.content.comment.infrastructure.mapper.CommentOperationLogMapper">

    <insert id="insert" parameterType="com.bluenote.content.comment.infrastructure.entity.CommentOperationLogEntity">
        INSERT INTO bluenote_comment.comment_operation_log (
            id, comment_id, user_id, operation_type, before_status,
            after_status, trace_id, created_at
        ) VALUES (
            #{id}, #{commentId}, #{userId}, #{operationType}, #{beforeStatus},
            #{afterStatus}, #{traceId}, #{createdAt}
        )
    </insert>
</mapper>
